# MCP Resources

Place text, markdown, JSON, CSV, or code files here when you want the MCP server to read them for summarization or analysis.

This directory is mounted read-only into the MCP container at:

/mnt/mcp-resources
